// Get Image
curl -v localhost:8000/static/rahul_prof.png

// Get User's Contacts
curl -v 'localhost:8000/users/uuid-1/people?searchtext=rei&maxpeople=5'

// Get Group's People
curl -v 'localhost:8000/groups/uuid-1/people?searchtext=eb&maxpeople=5'

// Update Group

// Create Group